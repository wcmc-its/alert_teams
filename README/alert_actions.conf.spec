[teams]
param.url = <string>
* webhook for the teams channel you're sending to

param.message = <string>
* markdown-formatted message

param.send_facts = [ true | false ]
* send the fields from the generating search to teams as a list of "facts"
* to be used in a formatted table
